/*
   if you want to use this vim moden IDE, you need to 
install the following software:
   1.  pip install -u pep8       # if you use python3, pip3 instead
   2.  apt-get install ack-grep
   3.  apt-get install ctags
   4.  apt-get python-mode
*/

        
        then you can do this to get the submodules:
               1.  cp -R ~/.vim/odl/. ~/.vim/
               2.  rm -rf ~/.vim/odl/
               3.  ln -s ~/.vim/vimrc ~/.vimrc
               4.  git submodule init
               5.  git submodule update


        ============================================
              LaiDongLiang   2016-5-6
        ============================================
